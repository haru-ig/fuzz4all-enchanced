#include <stdlib.h>


int main() {
    const int a = 10;
    const int b = 5;

     
    if (a < b) {
        const int difference = a - b;
         
        if (a != b) {
            const int sum = a + b;
            printf("Sum: %d", sum);
            const char* result;
             
            switch (sum % 2) {
                case 0:
                    result = "even";
                    break;
                case 1:
                    result = "odd";
                    break;
                default:
                    result = "unknown";
            }
             
            int result_number = (sum > 10) ? 20 : 30;
            printf("Result: %s - %d", result, result_number);
        }
         
        break;
    } else {
        const char* message = "Hello, World!";
        printf("Message: %s", message);
         
        continue;
    }

     
    goto end;

     
end:
    printf("End of program");

    return 0;
}

#include <stdio.h>

int main() {
    int x = 0;
    int y = 0;

     
    if (x < y) {
         
        while (x < 5) {
            printf("x: %d, y: %d", x, y);
            x++;
            y++;
             
            for (int i = 0; i < 3; i++) {
                printf("i: %d", i);
            }
        }
    } else {
         
        do {
            printf("x: %d, y: %d", x, y);
            x--;
            y--;
        } while (x > 0 && y > 0);
    }

    return 0;
}

*/